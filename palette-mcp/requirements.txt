# Palette MCP Server Dependencies
#
# This module uses the root requirements.txt for all dependencies.
# Install from project root: pip install -r requirements.txt
#
# Core dependencies used by this module:
# - mcp, fastmcp: MCP server framework
# - httpx, aiohttp: HTTP clients for Palette API
# - pydantic, pyyaml: Data validation and policy parsing
# - openai, anthropic: LLM integration for AI diagnostics

-r ../requirements.txt
